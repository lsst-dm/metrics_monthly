includeConfigs:
- ${CTRL_BPS_PANDA_DIR}/config/bps_usdf.yaml
- requestMemory.yaml

pipelineYaml: "${ANALYSIS_TOOLS_DIR}/pipelines/coaddQualityCore.yaml"


LSST_VERSION: w_2022_36
payload:
  payloadName: "{LSST_VERSION}/DM-36066"
  output: "HSC/runs/RC2/{payloadName}"
  butlerConfig: /sdf/group/rubin/repo/main_20220411
  inCollection: "HSC/RC2/defaults"

  dataQuery: ""

executionButler:
  requestMemory: 64000
  queue: "SLAC_Rubin_Merge"


numberOfRetries: 3

# For rescue
#extraQgraphOptions: "--clobber-outputs --skip-existing-in {output}"
